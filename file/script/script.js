

function likes () {
    let like = document.querySelector('.like')
let res = document.querySelector('.likeContagem')
let result = Number(res)
    if(result.innerHTML = 0){
       res.innerHTML=`${res}` 
    }
    res.innerHTML+=`mais ${res} gosteis` 
}